# Initalize with git and github
1. Open terminal with Ctrl+Shift+T
2. Copy this in
```bash
sudo apt-get update &&
sudo apt-get install -y git &&
git clone https://github.com/valdisxp1/my-reinstall.git &&
cd my-reinstall
```


